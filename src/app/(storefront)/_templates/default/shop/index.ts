import type { TemplateField, TemplateFieldGroup } from "~/lib/template-fields";

// No owner-editable fields for the shop pages yet.
// Add fields here as the pages evolve (e.g. shop hero heading, promo banner).

export const defaultShopData: TemplateField[] = [];

export const defaultShopFieldGroups: TemplateFieldGroup[] = [];
