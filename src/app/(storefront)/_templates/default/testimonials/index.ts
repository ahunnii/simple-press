import type { TemplateField, TemplateFieldGroup } from "~/lib/template-fields";

// No owner-editable fields for the testimonials pages yet.
// Add fields here as the pages evolve (e.g. page heading, CTA copy).

export const defaultTestimonialsData: TemplateField[] = [];

export const defaultTestimonialsFieldGroups: TemplateFieldGroup[] = [];
